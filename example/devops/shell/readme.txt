# 远程模式：从远程仓库拉取代码并构建部署
RUN_ENV=${RUN_ENV} \
PROJECT_NAME=${PROJECT_NAME} \
DEPLOYMENT_NAME=${DEPLOYMENT_NAME} \
NAMESPACE=${NAMESPACE} \
BRANCH=${BRANCH} \
GIT_URL=${GIT_URL} \
GIT_USERNAME=${GIT_USERNAME} \
GIT_PASSWORD=${GIT_PASSWORD} \
DOCKER_USER=${DOCKER_USER} \
DOCKER_PWD=${DOCKER_PWD} \
IMAGE_REGISTRY=${IMAGE_REGISTRY} \
DEPLOY_IMAGE_NETWORK=${DEPLOY_IMAGE_NETWORK} \
bash build.sh

# 本地模式：使用本地代码目录构建并部署
CODE_DIR=${CODE_DIR} \
RUN_ENV=${RUN_ENV} \
PROJECT_NAME=${PROJECT_NAME} \
DEPLOYMENT_NAME=${DEPLOYMENT_NAME} \
NAMESPACE=${NAMESPACE} \
BRANCH=${BRANCH} \
DOCKER_USER=${DOCKER_USER} \
DOCKER_PWD=${DOCKER_PWD} \
IMAGE_REGISTRY=${IMAGE_REGISTRY} \
DEPLOY_IMAGE_NETWORK=${DEPLOY_IMAGE_NETWORK} \
bash build.sh
